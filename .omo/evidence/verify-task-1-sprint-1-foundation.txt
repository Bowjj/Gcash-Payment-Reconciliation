ADVERSARIAL VERIFICATION - TASK 1 / SPRINT 1 FOUNDATION
Date: 2026-09-21
Verifier scope: Plan checkbox 1 only, independently reproduced.
Verdict: needs-fix

REQUIRED REPRODUCTION
- `corepack pnpm install --frozen-lockfile`: exit 0. pnpm 12.5.1 reported a current lockfile and passed supply-chain policies.
- `corepack pnpm typecheck`: exit 0 (`tsc --noEmit`).
- `corepack pnpm lint`: exit 0 (`eslint . --max-warnings=0`).
- `corepack pnpm build`: exit 0. Next.js 16.3.5 Turbopack compiled, type-checked, generated four static pages, and listed `/`, `/_not-found`, and `/icon.svg`.
- Build-output probe: the just-built `.next/BUILD_ID` contains `k-EIiugcSbTKvAaE-rwez`; `.next/app-path-routes-manifest.json` maps `/page` to `/`; `.next/server/app/page.js` exists. This is real emitted output, not success text alone.
- Started `corepack pnpm start --hostname 127.0.0.1`, then ran `curl.exe -I http://127.0.0.1:3000`. Result: `HTTP/1.1 200 OK`, `X-Powered-By: Next.js`, `Content-Length: 7010`.
- Killed the complete spawned process tree with `taskkill.exe /PID 12452 /T /F`. Follow-up `Get-NetTCPConnection -LocalPort 3000 -State Listen` found zero listeners.
- `corepack pnpm test`: exit 1 with `Unit test harness is added in Wave 2.`
- `corepack pnpm test:e2e`: exit 1 with `E2E test harness is added in Wave 2.` These are clear intentional failures, not false passes, and are allowed before Todo 4.

STRICTNESS AND STATIC AUDIT
- Inspected all Wave 1 source files: `src/app/{globals.css,icon.svg,layout.tsx,page.tsx}`, `src/lib/utils.ts`, and `src/components/ui/{button,input,label,card}.tsx`.
- Inspected all product/config files: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `tsconfig.json`, `eslint.config.mjs`, `next.config.ts`, `postcss.config.mjs`, `components.json`, `.npmrc`, `.gitignore`, and `.editorconfig`.
- `tsconfig.json:7` enables `strict: true` and also enables `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitReturns`, unused-symbol checks, and other strict compiler flags.
- `eslint.config.mjs:10-16` errors on TypeScript suppression comments, explicit `any`, non-null assertions, and type assertions. Text search found no suppression comments, `as any`, or `: any` in product scope.
- TypeScript LSP diagnostics could not run because the TypeScript LSP is not installed. The required fallback gates, strict `tsc --noEmit` and ESLint, both passed.
- ast-grep structural scans of `src/` for `$VALUE as any` and `$VALUE!` each exited 1 with no matches. `rg` is unavailable on this host, so the built-in repository grep performed the text searches instead.
- No fake-pass scripts were found. The required executable scripts are actual Next/ESLint/TypeScript commands; the future test scripts deliberately fail as above.

SCOPE, METADATA, AND DEPENDENCIES
- Package/runtime reproduced: Node v24.19.0, pnpm 12.5.1, Next.js v16.3.5; `package.json` requires Node `>=22.12.0` and pins `pnpm@12.5.1`.
- No `supabase/`, `tests/`, or `scripts/` directories exist. Product/config and source searches found no Supabase/auth/database, test-harness, dashboard, upload/import, ExcelJS/XLSX, GCash, reconciliation workflow, export/report, Main List, subscription, or billing implementation. The only product wording is the neutral foundation title/description.
- `corepack pnpm why exceljs` returned no package output (exit 0); `package.json` has no ExcelJS, Supabase, database, auth, test, or other Sprint 2+ dependency.
- Dependency review: Next/React/React DOM power the app; Tailwind/PostCSS power `globals.css`; TypeScript and ESLint power required gates; `@base-ui/react`, `class-variance-authority`, and `cn` are imported by generated primitives; `shadcn` and `tw-animate-css` are imported by `globals.css`.
- FINDING: `package.json:22` declares direct production dependency `lucide-react@1.47.0`. `corepack pnpm why lucide-react` shows only the root importer, and source grep found no `lucide-react` import. None of the four generated primitives imports it. This is dependency bloat against the Todo 1 requirement to add only required shadcn primitives/current-app dependencies. Remove it or add a real Wave 1 use only when later scoped work requires it. This finding makes `confirmed` invalid.
- `.codegraph/` remains present with `codegraph.db`, WAL/SHM, `.gitignore`, and `source.json`; `.omo/` remains present with plan, boulder state, notepads, evidence, ledger, and continuation metadata. The plan checkbox remains unchecked at `.omo/plans/sprint-1-foundation.md:66`; boulder state still names the plan. No Git repository exists: `git rev-parse --is-inside-work-tree` exited 128. Dirty-worktree classification is therefore not applicable.

CONCLUSION
- The runtime and deterministic tooling claims are independently reproduced and pass.
- The DoneClaim is not confirmed solely because of the unused direct `lucide-react` production dependency at `package.json:22`.

SECOND-ROUND ADVERSARIAL RE-VERIFICATION - 2026-09-21
Verdict: confirmed

- Read the first-round verifier result above and the executor's appended fix evidence in `.omo/evidence/task-1-sprint-1-foundation.txt:135-173`; this round independently reproduced every result below.
- Prior defect resolved: `lucide-react` is absent from `package.json`, has no match anywhere in `pnpm-lock.yaml`, has no `node_modules` package manifest, and `corepack pnpm why lucide-react` produced no package output with exit 0. `corepack pnpm list --depth 0` lists 16 packages and excludes it.
- Re-audited every direct dependency: Next/React/React DOM are the application runtime; `@base-ui/react`, `class-variance-authority`, and `cn` are imported by generated primitives; `shadcn` and `tw-animate-css` are CSS imports; Tailwind/PostCSS, TypeScript, ESLint, eslint-config-next, and type packages are required build/tool gates. No replacement direct dependency bloat exists.
- `corepack pnpm install --frozen-lockfile`: exit 0. `corepack pnpm typecheck`: exit 0. `corepack pnpm lint`: exit 0. `corepack pnpm build`: exit 0 with Next.js 16.3.5 Turbopack compiling, type-checking, and generating four static pages.
- Fresh build proof: `.next/BUILD_ID` is `EhskKRXkI0DsQzScf_ae_`; `.next/app-path-routes-manifest.json` maps `/page` to `/`; `.next/server/app/page.js` exists.
- Production QA: started `corepack pnpm start --hostname 127.0.0.1`; `curl.exe -I http://127.0.0.1:3000` returned `HTTP/1.1 200 OK`, `X-Powered-By: Next.js`, and `Content-Length: 7010`. Terminated the complete process tree with `taskkill.exe /PID 15964 /T /F`; process inspection then reported `PORT_3000_LISTENERS=0`.
- Future harness scripts remain honest: `corepack pnpm test` and `corepack pnpm test:e2e` each exit 1 with their explicit Wave 2 harness messages, rather than fake-passing.
- Static scope regression search found no removed-package import, strictness bypass, or Sprint 2+ source/package implementation. TypeScript LSP diagnostics remain unavailable because the server is not installed; the required strict `tsc --noEmit` and ESLint fallback gates passed.
- The first-round `needs-fix` history is retained above. No product/config/plan/boulder/notepad file was changed during this re-verification; this evidence append is the only verifier write.
